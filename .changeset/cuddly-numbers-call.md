---
"@sv443-network/userutils": patch
---

Updated CoreUtils to v3.1.0.  
This fixes various DataStore and DataStoreEngine issues. It also adds a second parameter of type `DataStoreEngineDSOptions` to the `filePath` function property in `FileStorageEngineOptions`, which allows for setting the file path based on encoding options.
